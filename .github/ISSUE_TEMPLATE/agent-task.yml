name: Agent task
description: A scoped, actionable task an AI agent (Claude Code, Copilot) can pick up.
title: "[Task]: "
labels: ["agent-ready"]
body:
  - type: markdown
    attributes:
      value: |
        After submitting, manually apply exactly one `size:*` label and one `risk:*` label. The dropdown answers below document the choice but do not add labels; those labels are required for daily-agent eligibility.

  - type: textarea
    id: summary
    attributes:
      label: What needs to happen
      description: One or two sentences. Be concrete about the end state, not the motivation.
    validations:
      required: true

  - type: textarea
    id: acceptance
    attributes:
      label: Acceptance criteria
      description: How to know it's done. Bullet list, testable where possible.
      placeholder: |
        - [ ] ...
        - [ ] ...
    validations:
      required: true

  - type: textarea
    id: scope
    attributes:
      label: Files / areas touched
      description: Known files, folders, or classes this task expects to touch.

  - type: dropdown
    id: risk
    attributes:
      label: Risk level
      description: Choose the matching risk label to apply after submission; definitions are summarized in agent-plan.md.
      options:
        - read (no state change)
        - reversible (easy-to-undo local change)
        - sensitive (private context or meaningful external impact)
        - destructive (requires human execution, not agent-eligible)
      default: 1
    validations:
      required: true

  - type: dropdown
    id: size
    attributes:
      label: Size estimate
      options:
        - S (single file, under an hour)
        - M (several files, a focused session)
        - L (multi-session, needs a plan first)
      default: 0
    validations:
      required: true

  - type: textarea
    id: context
    attributes:
      label: Additional context
      description: Links to related issues, agent-plan.md, ROAD MAP.md, or prior discussion.
